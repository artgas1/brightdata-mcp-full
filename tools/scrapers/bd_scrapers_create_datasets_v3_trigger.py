"""bd_scrapers_create_datasets_v3_trigger — auto-generated tool for POST /datasets/v3/trigger.

Auto-generated by lib/codegen.py. DO NOT EDIT BY HAND.
To regenerate: `uv run python scripts/regenerate.py`.

Source spec: scraper-rest-api,crawl-rest-api
"""
# ruff: noqa
# pyright: ignore

from __future__ import annotations

from typing import Any

from lib.client import bd_request
from lib.safety import requires_write_enabled

GROUP = "scrapers"
WRITES = True


@requires_write_enabled
async def bd_scrapers_create_datasets_v3_trigger(dataset_id: str, custom_output_fields: str | None = None, type: str | None = None, discover_by: str | None = None, include_errors: bool | None = None, limit_per_input: float | None = None, limit_multiple_results: float | None = None, notify: str | None = None, endpoint: str | None = None, format: str | None = None, auth_header: str | None = None, uncompressed_webhook: bool | None = None, body: dict[str, Any] | None = None) -> dict[str, Any]:
    """Trigger a web scraping collection based on provided inputs

    From: POST /datasets/v3/trigger
    Docs: https://docs.brightdata.com/api-reference/
    Source spec: scraper-rest-api,crawl-rest-api

    Args:
            dataset_id: Dataset ID for which data collection is triggered. Read more about [Dataset ID](/api-reference/terminology#dataset-id).
            custom_output_fields: The "custom_output_fields" parameter is used to filter the response data to include only the specified fields. You can list the output columns you want, separated by a pipe (`|`).    For example, if you want the response to include only the URL and the date it was last updated, you would set the parameter to "url|about.updated_on". This allows you to customize the data output to include only the fields relevant to your needs.
            type: Set it to "discover_new" to trigger a collection that includes a discovery phase.    Enables a discovery phase that finds new entities or products using methods like search, categories, or keywords. Use this when collecting data where specific targets aren't known in advance. It will discover new information based on your provided inputs rather than working with predefined data points.
            discover_by: Specifies the method used for discovering new data during a collection. Here are some available options:    - `keyword`: Uses keywords to discover new entities or products. Example: "smartphones" - This will trigger a collection to discover new smartphone products or entities.   - `best_sellers_url`: Uses a URL that lists best-selling items to discover new products.  Example: "https://example.com/best-sellers" - This URL will be used to discover products listed as best sellers on the site.   - `category_url`: Uses a URL that lists categories to discover new entities within those categories. Example: "https://example.com/electronics" - This URL will be used to discover new products within the electronics category.   - `location`: Uses a location-based approach to discover entities relevant to that location. Example: "New York" - This will trigger a collection to discover data related to the specified location.
            include_errors: Include errors report with the results. By setting "include_errors" to `true`, you will receive a detailed report of any errors that occur during the data collection.
            limit_per_input: Limit the number of results per input
            limit_multiple_results: Limit the total number of results
            notify: Specify whether notifications should be sent upon completion of the data collection job. When set to `true`, it enables notifications to be sent to the specified webhook, informing you about the status or completion of the collection.
            endpoint: Specify the Webhook URL that should be called for the data collection process.
            format: Specifies the format of the data to be delivered
            auth_header: Authorization header for webhook delivery
            uncompressed_webhook: By default, the data will be sent compressed. Pass true to send it uncompressed
            body: JSON request body. See BD docs for schema.

    Returns:
        Parsed JSON response, or `{"error": str, "status_code": int, ...}` on failure.
    """
    path = "/datasets/v3/trigger"
    params: dict[str, Any] = {}
    params["dataset_id"] = dataset_id
    if custom_output_fields is not None:
        params["custom_output_fields"] = custom_output_fields
    if type is not None:
        params["type"] = type
    if discover_by is not None:
        params["discover_by"] = discover_by
    if include_errors is not None:
        params["include_errors"] = include_errors
    if limit_per_input is not None:
        params["limit_per_input"] = limit_per_input
    if limit_multiple_results is not None:
        params["limit_multiple_results"] = limit_multiple_results
    if notify is not None:
        params["notify"] = notify
    if endpoint is not None:
        params["endpoint"] = endpoint
    if format is not None:
        params["format"] = format
    if auth_header is not None:
        params["auth_header"] = auth_header
    if uncompressed_webhook is not None:
        params["uncompressed_webhook"] = uncompressed_webhook
    return await bd_request("POST", path, params=params, json=body)
