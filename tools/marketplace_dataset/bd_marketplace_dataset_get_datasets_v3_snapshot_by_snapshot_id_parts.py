"""bd_marketplace_dataset_get_datasets_v3_snapshot_by_snapshot_id_parts — auto-generated tool for GET /datasets/v3/snapshot/{snapshot_id}/parts.

Auto-generated by lib/codegen.py. DO NOT EDIT BY HAND.
To regenerate: `uv run python scripts/regenerate.py`.

Source spec: dca-api,datasets-rest-api
"""
# ruff: noqa
# pyright: ignore

from __future__ import annotations

from typing import Any

from lib.client import bd_request

GROUP = "marketplace_dataset"
WRITES = False


async def bd_marketplace_dataset_get_datasets_v3_snapshot_by_snapshot_id_parts(
    snapshot_id: str,
    format: str | None = None,
    compress: bool | None = None,
    batch_size: int | None = None,
) -> dict[str, Any]:
    """When requesting a delivery in batches (see available delivery APIs) use this endpoint to check how many parts were created. format, compress and batch_size should exactly match what was sent to the de

    When requesting a delivery in batches (see available delivery APIs) use this endpoint to
    check how many parts were created. format, compress and batch_size should exactly match
    what was sent to the delivery/download API calls.

    From: GET /datasets/v3/snapshot/{snapshot_id}/parts
    Docs: https://docs.brightdata.com/api-reference/
    Source spec: dca-api,datasets-rest-api

    Args:
            snapshot_id: Path parameter.
            format: Format of the data to be received
            compress: Whether the result should be compressed or not
            batch_size: Divide into batches of X records

    Returns:
        Parsed JSON response, or `{"error": str, "status_code": int, ...}` on failure.
    """
    path = f"/datasets/v3/snapshot/{snapshot_id}/parts"
    params: dict[str, Any] = {}
    if format is not None:
        params["format"] = format
    if compress is not None:
        params["compress"] = compress
    if batch_size is not None:
        params["batch_size"] = batch_size
    return await bd_request("GET", path, params=params)
