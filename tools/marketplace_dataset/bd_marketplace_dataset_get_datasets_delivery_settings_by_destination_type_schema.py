"""bd_marketplace_dataset_get_datasets_delivery_settings_by_destination_type_schema — auto-generated tool for GET /datasets/delivery_settings/{destination_type}/schema.

Auto-generated by lib/codegen.py. DO NOT EDIT BY HAND.
To regenerate: `uv run python scripts/regenerate.py`.

Source spec: dca-api,datasets-rest-api
"""
# ruff: noqa
# pyright: ignore

from __future__ import annotations

from typing import Any

from lib.client import bd_request

GROUP = "marketplace_dataset"
WRITES = False


async def bd_marketplace_dataset_get_datasets_delivery_settings_by_destination_type_schema(
    destination_type: str,
) -> dict[str, Any]:
    """Returns the exact fields required for a specific delivery destination type (for example `s3`, `sftp`, `gcs`, `webhook`). Use this to discover which credentials and parameters must be supplied when upd

    Returns the exact fields required for a specific delivery destination type (for example
    `s3`, `sftp`, `gcs`, `webhook`). Use this to discover which credentials and parameters
    must be supplied when updating a view's delivery settings.

    From: GET /datasets/delivery_settings/{destination_type}/schema
    Docs: https://docs.brightdata.com/api-reference/
    Source spec: dca-api,datasets-rest-api

    Args:
            destination_type: The identifier of the delivery strategy. Supported values: `api_pull`, `webhook`, `email`, `gcs`, `gcp_pubsub`, `s3`, `snowflake`, `ali_oss`, `sftp`, `azure`.

    Returns:
        Parsed JSON response, or `{"error": str, "status_code": int, ...}` on failure.
    """
    path = f"/datasets/delivery_settings/{destination_type}/schema"
    return await bd_request("GET", path)
