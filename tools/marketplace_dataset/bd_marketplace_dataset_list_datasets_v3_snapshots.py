"""bd_marketplace_dataset_list_datasets_v3_snapshots — auto-generated tool for GET /datasets/v3/snapshots.

Auto-generated by lib/codegen.py. DO NOT EDIT BY HAND.
To regenerate: `uv run python scripts/regenerate.py`.

Source spec: dca-api,datasets-rest-api
"""
# ruff: noqa
# pyright: ignore

from __future__ import annotations

from typing import Any

from lib.client import bd_request

GROUP = "marketplace_dataset"
WRITES = False


async def bd_marketplace_dataset_list_datasets_v3_snapshots(
    dataset_id: str,
    status: str | None = None,
    skip: int | None = None,
    limit: int | None = None,
    from_date: str | None = None,
    to_date: str | None = None,
    with_total: bool | None = None,
    trigger_type: str | None = None,
) -> dict[str, Any]:
    """Get a list of triggered collections, the list contains only snapshots created for a specific dataset

    From: GET /datasets/v3/snapshots
    Docs: https://docs.brightdata.com/api-reference/
    Source spec: dca-api,datasets-rest-api

    Args:
            dataset_id: The dataset identifier (can be found in the specific API page)
            status: List only snapshots with a specific status
            skip: Skip the first `x` snapshots
            limit: Limit the number of snapshots to return
            from_date: List only snapshots that were created after a specific date
            to_date: List only snapshots that were created before a specific date
            with_total: Returns the total number of snapshots if this parameter is included
            trigger_type: Filter snapshots by type

    Returns:
        Parsed JSON response, or `{"error": str, "status_code": int, ...}` on failure.
    """
    path = "/datasets/v3/snapshots"
    params: dict[str, Any] = {}
    params["dataset_id"] = dataset_id
    if status is not None:
        params["status"] = status
    if skip is not None:
        params["skip"] = skip
    if limit is not None:
        params["limit"] = limit
    if from_date is not None:
        params["from_date"] = from_date
    if to_date is not None:
        params["to_date"] = to_date
    if with_total is not None:
        params["with_total"] = with_total
    if trigger_type is not None:
        params["trigger_type"] = trigger_type
    return await bd_request("GET", path, params=params)
