"""bd_marketplace_dataset_get_datasets_v3_snapshot_by_snapshot_id — auto-generated tool for GET /datasets/v3/snapshot/{snapshot_id}.

Auto-generated by lib/codegen.py. DO NOT EDIT BY HAND.
To regenerate: `uv run python scripts/regenerate.py`.

Source spec: dca-api,datasets-rest-api
"""
# ruff: noqa
# pyright: ignore

from __future__ import annotations

from typing import Any

from lib.client import bd_request

GROUP = "marketplace_dataset"
WRITES = False


async def bd_marketplace_dataset_get_datasets_v3_snapshot_by_snapshot_id(
    snapshot_id: str,
    format: str | None = None,
    compress: bool | None = None,
    batch_size: int | None = None,
    part: int | None = None,
) -> dict[str, Any]:
    """Download the snapshot content. This endpoint allows you to retrieve the collected dataset once the snapshot status is `ready`. Data can be downloaded in multiple formats and optionally split into batc

    Download the snapshot content. This endpoint allows you to retrieve the collected
    dataset once the snapshot status is `ready`. Data can be downloaded in multiple formats
    and optionally split into batches.

    From: GET /datasets/v3/snapshot/{snapshot_id}
    Docs: https://docs.brightdata.com/api-reference/
    Source spec: dca-api,datasets-rest-api

    Args:
            snapshot_id: The ID that was returned when the collection was triggered
            format: Format of the data to be received
            compress: Whether the result should be compressed or not
            batch_size: Divide the snapshot into batches of X records
            part: If `batch_size` was provided, specifies which batch part to download

    Returns:
        Parsed JSON response, or `{"error": str, "status_code": int, ...}` on failure.
    """
    path = f"/datasets/v3/snapshot/{snapshot_id}"
    params: dict[str, Any] = {}
    if format is not None:
        params["format"] = format
    if compress is not None:
        params["compress"] = compress
    if batch_size is not None:
        params["batch_size"] = batch_size
    if part is not None:
        params["part"] = part
    return await bd_request("GET", path, params=params)
