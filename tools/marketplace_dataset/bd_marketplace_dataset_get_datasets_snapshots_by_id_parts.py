"""bd_marketplace_dataset_get_datasets_snapshots_by_id_parts — auto-generated tool for GET /datasets/snapshots/{id}/parts.

Auto-generated by lib/codegen.py. DO NOT EDIT BY HAND.
To regenerate: `uv run python scripts/regenerate.py`.

Source spec: dca-api,datasets-rest-api
"""
# ruff: noqa
# pyright: ignore

from __future__ import annotations

from typing import Any

from lib.client import bd_request

GROUP = "marketplace_dataset"
WRITES = False


async def bd_marketplace_dataset_get_datasets_snapshots_by_id_parts(
    id: str, format: str | None = None, compress: bool | None = None, batch_size: int | None = None
) -> dict[str, Any]:
    """Get dataset snapshot delivery parts. All query parameters used here need to match those used when downloading the snapshot to get accurate parts

    From: GET /datasets/snapshots/{id}/parts
    Docs: https://docs.brightdata.com/api-reference/
    Source spec: dca-api,datasets-rest-api

    Args:
            id: A Snapshot ID is a unique identifier for a specific data snapshot, used to retrieve results from a data collection job triggered via the API. Read more about [Snapshot ID](/api-reference/terminology#snapshot-id).
            format: Format of the response
            compress: Whether or not the response will be compressed in gzip format
            batch_size: Number of records that will be included in each response batch

    Returns:
        Parsed JSON response, or `{"error": str, "status_code": int, ...}` on failure.
    """
    path = f"/datasets/snapshots/{id}/parts"
    params: dict[str, Any] = {}
    if format is not None:
        params["format"] = format
    if compress is not None:
        params["compress"] = compress
    if batch_size is not None:
        params["batch_size"] = batch_size
    return await bd_request("GET", path, params=params)
