"""bd_misc_create_datasets_filter — auto-generated tool for POST /datasets/filter.

Auto-generated by lib/codegen.py. DO NOT EDIT BY HAND.
To regenerate: `uv run python scripts/regenerate.py`.

Source spec: async-api-reference,dca-custom-inputs,filter-csv-json
"""
# ruff: noqa
# pyright: ignore

from __future__ import annotations

from typing import Any

from lib.client import bd_request
from lib.safety import requires_write_enabled

GROUP = "misc"
WRITES = True


@requires_write_enabled
async def bd_misc_create_datasets_filter(dataset_id: str, records_limit: int | None = None, body: dict[str, Any] | None = None) -> dict[str, Any]:
    """Create a dataset snapshot based on a provided filter

    From: POST /datasets/filter
    Docs: https://docs.brightdata.com/api-reference/
    Source spec: async-api-reference,dca-custom-inputs,filter-csv-json

    Args:
            dataset_id: ID of the dataset to filter (required in multipart/form-data mode)
            records_limit: Limit the number of records to be included in the snapshot
            body: JSON request body. See BD docs for schema.

    Returns:
        Parsed JSON response, or `{"error": str, "status_code": int, ...}` on failure.
    """
    path = "/datasets/filter"
    params: dict[str, Any] = {}
    params["dataset_id"] = dataset_id
    if records_limit is not None:
        params["records_limit"] = records_limit
    return await bd_request("POST", path, params=params, json=body)
